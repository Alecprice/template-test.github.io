import { readFile, writeFile } from 'node:fs/promises'

let sync = await readFile('src/lib/useAccountSync.ts', 'utf8')

const parseStart = sync.indexOf('function parseAccountState(value: unknown): AccountStateV1 | undefined {')
const parseEnd = sync.indexOf('\nfunction privateKey(', parseStart)
if (parseStart < 0 || parseEnd < 0) throw new Error('Cloud validation patch failed: parseAccountState block')

const replacement = `const MAX_CLOUD_STATE_BYTES = 524_288\nconst MAX_CLOUD_DEVICES = 64\nconst MAX_CLOUD_ACTIVITIES = 250\nconst MAX_CLOUD_STEPS = 120\nconst MAX_CLOUD_ID_LENGTH = 160\nconst MAX_CLOUD_NAME_LENGTH = 200\nconst MAX_CLOUD_ROOM_LENGTH = 200\nconst MAX_CLOUD_BRIDGE_URL_LENGTH = 2_048\n\nfunction isCloudRecord(value: unknown): value is Record<string, unknown> {\n  return Boolean(value) && typeof value === 'object' && !Array.isArray(value)\n}\n\nfunction boundedCloudText(value: unknown, maxLength: number, allowEmpty = false) {\n  return typeof value === 'string' && value.length <= maxLength && (allowEmpty || value.trim().length > 0)\n}\n\nfunction parseAccountState(value: unknown): AccountStateV1 | undefined {\n  try {\n    if (!isCloudRecord(value)) return undefined\n    const serialized = JSON.stringify(value)\n    if (serialized.length > MAX_CLOUD_STATE_BYTES) return undefined\n    const raw = value as Partial<AccountStateV1>\n    if (raw.schemaVersion !== 1 || !Array.isArray(raw.devices) || !Array.isArray(raw.activities)) return undefined\n    if (raw.devices.length > MAX_CLOUD_DEVICES || raw.activities.length > MAX_CLOUD_ACTIVITIES) return undefined\n\n    const deviceIds = new Set<string>()\n    const devices: TvDevice[] = []\n    for (const entry of raw.devices) {\n      if (!isCloudRecord(entry)) return undefined\n      const id = entry.id\n      const name = entry.name\n      const room = entry.room\n      const kind = entry.kind\n      if (!boundedCloudText(id, MAX_CLOUD_ID_LENGTH) || !boundedCloudText(name, MAX_CLOUD_NAME_LENGTH) || !boundedCloudText(room, MAX_CLOUD_ROOM_LENGTH, true)) return undefined\n      if (kind !== 'samsung' && kind !== 'firetv' && kind !== 'combo') return undefined\n      if (deviceIds.has(id)) return undefined\n      deviceIds.add(id)\n      devices.push(sanitizeDevice(entry as TvDevice))\n    }\n\n    const activityIds = new Set<string>()\n    const activities: Activity[] = []\n    for (const entry of raw.activities) {\n      if (!isCloudRecord(entry)) return undefined\n      const id = entry.id\n      const name = entry.name\n      const steps = entry.steps\n      if (!boundedCloudText(id, MAX_CLOUD_ID_LENGTH) || !boundedCloudText(name, MAX_CLOUD_NAME_LENGTH)) return undefined\n      if (!Array.isArray(steps) || steps.length > MAX_CLOUD_STEPS) return undefined\n      if (activityIds.has(id)) return undefined\n      activityIds.add(id)\n      activities.push(entry as Activity)\n    }\n\n    const requestedActive = boundedCloudText(raw.activeDeviceId, MAX_CLOUD_ID_LENGTH, true) ? raw.activeDeviceId as string : ''\n    const activeDeviceId = devices.some((device) => device.id === requestedActive) ? requestedActive : devices[0]?.id ?? ''\n    const bridgeUrl = boundedCloudText(raw.bridge?.url, MAX_CLOUD_BRIDGE_URL_LENGTH, true) ? raw.bridge!.url : ''\n\n    return {\n      schemaVersion: 1,\n      devices,\n      activities,\n      activeDeviceId,\n      preferences: {\n        haptics: raw.preferences?.haptics !== false,\n        keepAwake: raw.preferences?.keepAwake !== false,\n        appMode: raw.preferences?.appMode === 'kids' || raw.preferences?.appMode === 'light' || raw.preferences?.appMode === 'dark' ? raw.preferences.appMode : 'dark',\n        kidsAllowedStreamingIds: normalizeKidsStreamingIds(raw.preferences?.kidsAllowedStreamingIds),\n      },\n      bridge: { url: bridgeUrl },\n    }\n  } catch {\n    return undefined\n  }\n}\n`

sync = `${sync.slice(0, parseStart)}${replacement}${sync.slice(parseEnd)}`
await writeFile('src/lib/useAccountSync.ts', sync)
console.log('Defensive cloud account-state validation applied')
